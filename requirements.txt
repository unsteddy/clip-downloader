# requirements.txt

# Core dependencies (always install latest)
sqlalchemy
requests
streamlink

# Testing
unittest2; python_version<'3.0'

# Optional (for subtitle module)
# faster-whisper
# openai-whisper

# For environment management (optional)
# python-dotenv

# Oracle DB support (optional for production scaling)
# cx_Oracle

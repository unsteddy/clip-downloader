# db/__init__.py

# This file marks the 'db' directory as a Python package.
# It can optionally expose top-level functions or imports if needed.

# Example: make the Clip model accessible from db package
from .models import Clip
